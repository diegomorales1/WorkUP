import 'package:flutter/material.dart' ;

class ModPerfil extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Configuracion del perfil que no existe te odio firestore") ,
    );
  }
}